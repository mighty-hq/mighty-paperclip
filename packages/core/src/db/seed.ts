import { seedData } from '@mighty/core';

export const seedCategories = seedData.categories;
export const seedPromptCategories = seedData.promptCategories;
export const seedSnippets = seedData.snippets;
export const seedPrompts = seedData.prompts;
export const seedClipboard = seedData.clipboard;
export const seedSettings = seedData.settings;
